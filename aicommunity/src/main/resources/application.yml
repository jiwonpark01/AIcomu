server:
  port: 3000
  forward-headers-strategy: native

spring:
  application:
    name: aicommunity

  datasource:
    url: jdbc:mariadb://localhost:3306/aicomu?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: testuser
    password: 1234
    driver-class-name: org.mariadb.jdbc.Driver

  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql

logging:
  level:
    org.springframework.jdbc: DEBUG
